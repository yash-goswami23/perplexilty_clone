name: perplexilty_clone
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.7.6+2
  get: ^4.7.2
  google_fonts: ^6.2.1
  skeletonizer: ^1.4.3
  url_launcher: ^6.3.1
  web_socket_client: ^0.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
